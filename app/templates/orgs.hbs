<H1>My Favourite Orgs</H1>
  <ul>
    {{#each content as |org|}}
    <li>
      <span {{action 'favoriteClicked' org}}>
        [favorites ]
      </span>
      {{link-to org.id 'org' org.id }}
    </li>
    {{/each}}
  </ul>

{{outlet}}
